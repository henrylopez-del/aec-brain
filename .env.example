# Copia a .env para correr local. En Render esto va en el dashboard, NO en git.
GEMINI_API_KEY=tu_api_key_de_gemini
GEMINI_MODEL=gemini-flash-lite-latest
SYSTEM_PROMPT=Eres un asistente de atencion al cliente por WhatsApp...

# Supabase (prompts + memoria + estado pipeline)
SUPABASE_URL=https://fneppfjeywhayknrgahe.supabase.co
SUPABASE_SERVICE_ROLE_KEY=...

# Tenant por defecto (TVS) cuando el location no viene en el request
LOCATION_ID=V3iaVn0uBrQYxtNHPBeb
AGENT_KEY=tvs_ventas

# PIPELINE DE VENTAS estructurado: locations que lo usan (coma-separado).
# Estas leen company_context + prompts por etapa (agent_key='pipeline').
# El resto sigue con prompt unico. Ainnovation:
PIPELINE_LOCATIONS=ysOgfEqmLNpfGswNu3sO
PIPELINE_STATE_TABLE=aec_pipeline_state
